---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import GlassCard from '../components/GlassCard.astro';
import Button from '../components/Button.astro';
---

<BaseLayout 
  title="Liên hệ - Kết nối với hành trình"
  description="Liên hệ với người bộ hành để chia sẻ câu chuyện, gửi lời chúc, hoặc đồng hành cùng hành trình tỉnh thức xuyên Việt Nam."
>
  <Header slot="header" />

  <!-- Hero Section -->
  <section class="relative py-24 lg:py-32 overflow-hidden">
    <div class="absolute inset-0 z-0">
      <div 
        class="absolute inset-0 bg-cover bg-center"
        style="background-image: url('https://images.unsplash.com/photo-1506629905645-b178a0b13bc8?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80')"
      ></div>
      <div class="absolute inset-0 bg-gradient-to-b from-gray-900/70 via-gray-900/50 to-gray-900/70"></div>
    </div>

    <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold text-text-primary mb-8 leading-tight">
        <span class="text-gradient">Kết nối</span> với hành trình
      </h1>
      
      <p class="text-xl md:text-2xl text-text-secondary max-w-3xl mx-auto leading-relaxed">
        Hành trình này trở nên ý nghĩa hơn khi có sự đồng hành của bạn. 
        Hãy chia sẻ câu chuyện, gửi lời chúc, hoặc đơn giản chỉ là chào hỏi.
      </p>
    </div>
  </section>

  <!-- Contact Methods -->
  <section class="py-16 lg:py-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-text-primary mb-4">
          Cách thức kết nối
        </h2>
        <p class="text-lg text-text-secondary max-w-2xl mx-auto">
          Chọn cách thức phù hợp nhất để kết nối và chia sẻ với hành trình
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        
        <!-- Email -->
        <GlassCard variant="hover" padding="lg" class="text-center group animate-in animate-delay-100">
          <div class="w-16 h-16 bg-accent-color/20 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-accent-color" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-text-primary mb-3">Email</h3>
          <p class="text-text-secondary mb-4 text-sm">
            Gửi email chi tiết để chia sẻ suy nghĩ hoặc câu chuyện của bạn
          </p>
          <Button
            variant="outline"
            size="sm"
            href="mailto:contact@bohanhtinhthuc.com"
            class="w-full group-hover:bg-accent-color group-hover:text-gray-900 group-hover:border-accent-color"
          >
            Gửi email
          </Button>
        </GlassCard>

        <!-- Social Media -->
        <GlassCard variant="hover" padding="lg" class="text-center group animate-in animate-delay-200">
          <div class="w-16 h-16 bg-accent-color/20 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-accent-color" fill="currentColor" viewBox="0 0 24 24">
              <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-text-primary mb-3">Mạng xã hội</h3>
          <p class="text-text-secondary mb-4 text-sm">
            Theo dõi và tương tác trên Facebook, Instagram để cập nhật hàng ngày
          </p>
          <Button
            variant="outline"
            size="sm"
            href="#"
            target="_blank"
            class="w-full group-hover:bg-accent-color group-hover:text-gray-900 group-hover:border-accent-color"
          >
            Theo dõi
          </Button>
        </GlassCard>

        <!-- Phone/SMS -->
        <GlassCard variant="hover" padding="lg" class="text-center group animate-in animate-delay-300">
          <div class="w-16 h-16 bg-accent-color/20 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-accent-color" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-text-primary mb-3">Điện thoại</h3>
          <p class="text-text-secondary mb-4 text-sm">
            Gọi điện hoặc nhắn tin trong những trường hợp khẩn cấp
          </p>
          <Button
            variant="outline"
            size="sm"
            href="tel:+84123456789"
            class="w-full group-hover:bg-accent-color group-hover:text-gray-900 group-hover:border-accent-color"
          >
            Gọi điện
          </Button>
        </GlassCard>

        <!-- Physical Mail -->
        <GlassCard variant="hover" padding="lg" class="text-center group animate-in animate-delay-400">
          <div class="w-16 h-16 bg-accent-color/20 rounded-full flex items-center justify-center mx-auto mb-6 group-hover:scale-110 transition-transform duration-300">
            <svg class="w-8 h-8 text-accent-color" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-text-primary mb-3">Thư từ</h3>
          <p class="text-text-secondary mb-4 text-sm">
            Gửi thư, quà nhỏ hoặc tặng phẩm đến địa điểm hiện tại trên hành trình
          </p>
          <Button
            variant="outline"
            size="sm"
            href="#current-location"
            class="w-full group-hover:bg-accent-color group-hover:text-gray-900 group-hover:border-accent-color"
          >
            Xem vị trí
          </Button>
        </GlassCard>
      </div>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="py-16 lg:py-24 bg-gradient-to-b from-transparent to-gray-900/30">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-text-primary mb-4">
          Gửi lời nhắn
        </h2>
        <p class="text-lg text-text-secondary max-w-2xl mx-auto">
          Chia sẻ suy nghĩ, câu chuyện của bạn hoặc đơn giản chỉ là gửi lời chúc
        </p>
      </div>
      
      <GlassCard padding="xl">
        <form id="contact-form" class="space-y-6" novalidate>
          
          <!-- Name and Email Row -->
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label for="name" class="block text-sm font-medium text-text-primary mb-2">
                Họ và tên <span class="text-red-400">*</span>
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 glass-card text-text-primary placeholder-text-secondary border-white/20 rounded-lg focus:outline-none focus:ring-2 focus:ring-accent-color focus:border-accent-color transition-all duration-300"
                placeholder="Tên của bạn"
              />
              <p class="text-red-400 text-sm mt-1 hidden" id="name-error">Vui lòng nhập họ tên</p>
            </div>
            
            <div>
              <label for="email" class="block text-sm font-medium text-text-primary mb-2">
                Email <span class="text-red-400">*</span>
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 glass-card text-text-primary placeholder-text-secondary border-white/20 rounded-lg focus:outline-none focus:ring-2 focus:ring-accent-color focus:border-accent-color transition-all duration-300"
                placeholder="email@example.com"
              />
              <p class="text-red-400 text-sm mt-1 hidden" id="email-error">Vui lòng nhập email hợp lệ</p>
            </div>
          </div>

          <!-- Subject -->
          <div>
            <label for="subject" class="block text-sm font-medium text-text-primary mb-2">
              Chủ đề
            </label>
            <select
              id="subject"
              name="subject"
              class="w-full px-4 py-3 glass-card text-text-primary border-white/20 rounded-lg focus:outline-none focus:ring-2 focus:ring-accent-color focus:border-accent-color transition-all duration-300"
            >
              <option value="">Chọn chủ đề</option>
              <option value="encouragement">Lời động viên</option>
              <option value="story">Chia sẻ câu chuyện</option>
              <option value="question">Đặt câu hỏi</option>
              <option value="collaboration">Hợp tác</option>
              <option value="media">Truyền thông</option>
              <option value="other">Khác</option>
            </select>
          </div>

          <!-- Phone (Optional) -->
          <div>
            <label for="phone" class="block text-sm font-medium text-text-primary mb-2">
              Số điện thoại (tùy chọn)
            </label>
            <input
              type="tel"
              id="phone"
              name="phone"
              class="w-full px-4 py-3 glass-card text-text-primary placeholder-text-secondary border-white/20 rounded-lg focus:outline-none focus:ring-2 focus:ring-accent-color focus:border-accent-color transition-all duration-300"
              placeholder="+84 xxx xxx xxx"
            />
          </div>

          <!-- Message -->
          <div>
            <label for="message" class="block text-sm font-medium text-text-primary mb-2">
              Lời nhắn <span class="text-red-400">*</span>
            </label>
            <textarea
              id="message"
              name="message"
              required
              rows="6"
              class="w-full px-4 py-3 glass-card text-text-primary placeholder-text-secondary border-white/20 rounded-lg focus:outline-none focus:ring-2 focus:ring-accent-color focus:border-accent-color transition-all duration-300 resize-vertical"
              placeholder="Chia sẻ suy nghĩ, câu chuyện hoặc lời động viên của bạn..."
            ></textarea>
            <p class="text-red-400 text-sm mt-1 hidden" id="message-error">Vui lòng nhập lời nhắn</p>
            <p class="text-text-secondary text-xs mt-2">Tối thiểu 10 ký tự</p>
          </div>

          <!-- Privacy Notice -->
          <div class="bg-accent-color/5 border border-accent-color/20 rounded-lg p-4">
            <div class="flex items-start space-x-3">
              <svg class="w-5 h-5 text-accent-color mt-0.5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
              </svg>
              <div>
                <h4 class="text-text-primary font-medium mb-1">Bảo mật thông tin</h4>
                <p class="text-text-secondary text-sm">
                  Thông tin của bạn sẽ được bảo mật và chỉ sử dụng để phản hồi tin nhắn. 
                  Chúng tôi không chia sẻ thông tin cá nhân với bên thứ ba.
                </p>
              </div>
            </div>
          </div>

          <!-- Submit Button -->
          <div class="flex flex-col sm:flex-row items-center space-y-4 sm:space-y-0 sm:space-x-4">
            <Button
              type="submit"
              variant="primary"
              size="lg"
              class="w-full sm:w-auto px-8 py-3"
              id="submit-btn"
            >
              <span id="submit-text">Gửi tin nhắn</span>
              <span id="submit-loading" class="hidden">Đang gửi...</span>
            </Button>
            
            <Button
              type="reset"
              variant="ghost"
              size="lg"
              class="w-full sm:w-auto px-8 py-3"
            >
              Xóa form
            </Button>
          </div>

          <!-- Success Message -->
          <div id="success-message" class="hidden bg-green-500/10 border border-green-500/20 text-green-400 rounded-lg p-4">
            <div class="flex items-center space-x-3">
              <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
              <div>
                <h4 class="font-medium">Tin nhắn đã được gửi!</h4>
                <p class="text-sm">Cảm ơn bạn đã chia sẻ. Tôi sẽ phản hồi trong thời gian sớm nhất.</p>
              </div>
            </div>
          </div>

          <!-- Error Message -->
          <div id="error-message" class="hidden bg-red-500/10 border border-red-500/20 text-red-400 rounded-lg p-4">
            <div class="flex items-center space-x-3">
              <svg class="w-5 h-5 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
              </svg>
              <div>
                <h4 class="font-medium">Có lỗi xảy ra</h4>
                <p class="text-sm">Vui lòng thử lại sau hoặc liên hệ trực tiếp qua email.</p>
              </div>
            </div>
          </div>
        </form>
      </GlassCard>
    </div>
  </section>

  <!-- Current Location -->
  <section class="py-16 lg:py-24" id="current-location">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-text-primary mb-4">
          Vị trí hiện tại
        </h2>
        <p class="text-lg text-text-secondary max-w-2xl mx-auto">
          Theo dõi vị trí hiện tại của hành trình để gửi thư hoặc gặp gỡ trực tiếp
        </p>
      </div>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
        
        <!-- Location Info -->
        <div class="space-y-6">
          <GlassCard padding="lg" class="animate-in animate-delay-100">
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-accent-color/20 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg class="w-6 h-6 text-accent-color" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-text-primary mb-2">Đang ở</h3>
                <p class="text-text-secondary">
                  <strong class="text-text-primary">Thành phố Cà Mau, tỉnh Cà Mau</strong><br>
                  Gần khu vực Đất Mũi - điểm cực Nam của Việt Nam
                </p>
                <p class="text-xs text-accent-color mt-2">
                  Cập nhật: 15/01/2025
                </p>
              </div>
            </div>
          </GlassCard>

          <GlassCard padding="lg" class="animate-in animate-delay-200">
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-accent-color/20 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg class="w-6 h-6 text-accent-color" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-text-primary mb-2">Kế hoạch tiếp theo</h3>
                <p class="text-text-secondary">
                  <strong class="text-text-primary">Hướng về An Giang</strong><br>
                  Dự kiến đến ngày 22/01/2025<br>
                  Khoảng cách: ~80km (4 ngày đi bộ)
                </p>
              </div>
            </div>
          </GlassCard>

          <GlassCard padding="lg" class="animate-in animate-delay-300">
            <div class="flex items-start space-x-4">
              <div class="w-12 h-12 bg-accent-color/20 rounded-lg flex items-center justify-center flex-shrink-0">
                <svg class="w-6 h-6 text-accent-color" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                </svg>
              </div>
              <div>
                <h3 class="text-lg font-semibold text-text-primary mb-2">Gửi thư/quà</h3>
                <p class="text-text-secondary mb-3">
                  Nếu bạn muốn gửi thư hoặc quà nhỏ, vui lòng liên hệ trước qua email 
                  để biết địa chỉ chính xác và thời gian phù hợp.
                </p>
                <Button
                  variant="outline"
                  size="sm"
                  href="mailto:contact@bohanhtinhthuc.com?subject=Gửi thư/quà"
                >
                  Liên hệ gửi thư
                </Button>
              </div>
            </div>
          </GlassCard>
        </div>

        <!-- Map Placeholder -->
        <div class="animate-in animate-delay-400">
          <GlassCard padding="none" class="overflow-hidden">
            <div class="aspect-[4/3] bg-gradient-to-br from-accent-color/20 to-accent-color/5 flex items-center justify-center">
              <div class="text-center">
                <svg class="w-16 h-16 text-accent-color/60 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 20l-5.447-2.724A1 1 0 013 16.382V5.618a1 1 0 011.447-.894L9 7m0 13l6-3m-6 3V7m6 10l4.553 2.276A1 1 0 0021 18.382V7.618a1 1 0 00-.553-.894L15 4m0 13V4m0 0L9 7" />
                </svg>
                <h3 class="text-lg font-semibold text-text-primary mb-2">Bản đồ hành trình</h3>
                <p class="text-text-secondary text-sm mb-4">
                  Theo dõi lộ trình và vị trí hiện tại của hành trình
                </p>
                <Button variant="outline" size="sm">
                  Xem bản đồ đầy đủ
                </Button>
              </div>
            </div>
          </GlassCard>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="py-16 lg:py-24 bg-gradient-to-b from-gray-900/30 to-transparent">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-2xl md:text-3xl font-bold text-text-primary mb-4">
          Câu hỏi thường gặp
        </h2>
        <p class="text-lg text-text-secondary">
          Những câu hỏi phổ biến về hành trình và cách liên hệ
        </p>
      </div>
      
      <div class="space-y-6">
        
        <!-- FAQ Item 1 -->
        <GlassCard padding="lg" class="animate-in animate-delay-100">
          <details class="group">
            <summary class="flex items-center justify-between cursor-pointer">
              <h3 class="text-lg font-semibold text-text-primary">
                Tôi có thể gặp trực tiếp người bộ hành không?
              </h3>
              <svg class="w-5 h-5 text-accent-color group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="mt-4 text-text-secondary">
              <p>
                Tất nhiên! Tôi rất vui khi được gặp gỡ những người đồng hành. 
                Vui lòng liên hệ trước qua email hoặc điện thoại để sắp xếp thời gian và địa điểm phù hợp. 
                Lưu ý rằng lịch trình có thể thay đổi tùy thuộc vào thời tiết và tình hình thực tế.
              </p>
            </div>
          </details>
        </GlassCard>

        <!-- FAQ Item 2 -->
        <GlassCard padding="lg" class="animate-in animate-delay-200">
          <details class="group">
            <summary class="flex items-center justify-between cursor-pointer">
              <h3 class="text-lg font-semibold text-text-primary">
                Bao lâu thì nhận được phản hồi từ email?
              </h3>
              <svg class="w-5 h-5 text-accent-color group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="mt-4 text-text-secondary">
              <p>
                Thường trong vòng 2-3 ngày, tôi sẽ phản hồi email của bạn. 
                Do đang trên hành trình nên việc truy cập internet có thể bị hạn chế. 
                Trong trường hợp khẩn cấp, vui lòng gọi điện thoại trực tiếp.
              </p>
            </div>
          </details>
        </GlassCard>

        <!-- FAQ Item 3 -->
        <GlassCard padding="lg" class="animate-in animate-delay-300">
          <details class="group">
            <summary class="flex items-center justify-between cursor-pointer">
              <h3 class="text-lg font-semibold text-text-primary">
                Tôi có thể đồng hành một đoạn đường không?
              </h3>
              <svg class="w-5 h-5 text-accent-color group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="mt-4 text-text-secondary">
              <p>
                Rất hoan nghênh! Nhiều người đã đồng hành cùng tôi một đoạn đường ngắn. 
                Điều quan trọng là bạn cần chuẩn bị tinh thần cho việc đi bộ với tốc độ chậm (khoảng 3-4km/h) 
                và có thể thích nghi với lối sống tối giản trong thời gian đồng hành.
              </p>
            </div>
          </details>
        </GlassCard>

        <!-- FAQ Item 4 -->
        <GlassCard padding="lg" class="animate-in animate-delay-400">
          <details class="group">
            <summary class="flex items-center justify-between cursor-pointer">
              <h3 class="text-lg font-semibold text-text-primary">
                Làm sao để ủng hộ hành trình này?
              </h3>
              <svg class="w-5 h-5 text-accent-color group-open:rotate-180 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="mt-4 text-text-secondary">
              <p>
                Cách tốt nhất để ủng hộ là chia sẻ câu chuyện với bạn bè, gia đình để lan tỏa thông điệp sống tỉnh thức. 
                Bạn cũng có thể gửi lời động viên qua email hoặc mạng xã hội. 
                Nếu muốn ủng hộ vật chất, vui lòng liên hệ trực tiếp để biết cách thức phù hợp.
              </p>
            </div>
          </details>
        </GlassCard>
      </div>
    </div>
  </section>

  <Footer slot="footer" />
</BaseLayout>

<style>
  .text-gradient {
    background: linear-gradient(135deg, #34d399, #22c55e, #16a34a);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  
  /* Animation styles */
  .animate-in {
    animation: slideUp 0.6s ease-out forwards;
    opacity: 0;
    transform: translateY(20px);
  }
  
  .animate-delay-100 { animation-delay: 0.1s; }
  .animate-delay-200 { animation-delay: 0.2s; }
  .animate-delay-300 { animation-delay: 0.3s; }
  .animate-delay-400 { animation-delay: 0.4s; }
  
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  /* FAQ accordion styles */
  details summary {
    list-style: none;
  }
  
  details summary::-webkit-details-marker {
    display: none;
  }
  
  details[open] summary {
    margin-bottom: 1rem;
  }
  
  /* Form validation styles */
  .invalid {
    @apply border-red-400 focus:border-red-400 focus:ring-red-400;
  }
  
  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .animate-in {
      animation: none;
      opacity: 1;
      transform: none;
    }
    
    * {
      transition: none !important;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Contact form functionality
    const contactForm = document.getElementById('contact-form') as HTMLFormElement;
    const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
    const submitText = document.getElementById('submit-text');
    const submitLoading = document.getElementById('submit-loading');
    const successMessage = document.getElementById('success-message');
    const errorMessage = document.getElementById('error-message');
    
    // Form validation
    function validateForm() {
      let isValid = true;
      
      // Name validation
      const nameInput = document.getElementById('name') as HTMLInputElement;
      const nameError = document.getElementById('name-error');
      if (!nameInput.value.trim()) {
        nameInput.classList.add('invalid');
        nameError?.classList.remove('hidden');
        isValid = false;
      } else {
        nameInput.classList.remove('invalid');
        nameError?.classList.add('hidden');
      }
      
      // Email validation
      const emailInput = document.getElementById('email') as HTMLInputElement;
      const emailError = document.getElementById('email-error');
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!emailInput.value.trim() || !emailRegex.test(emailInput.value)) {
        emailInput.classList.add('invalid');
        emailError?.classList.remove('hidden');
        isValid = false;
      } else {
        emailInput.classList.remove('invalid');
        emailError?.classList.add('hidden');
      }
      
      // Message validation
      const messageInput = document.getElementById('message') as HTMLTextAreaElement;
      const messageError = document.getElementById('message-error');
      if (!messageInput.value.trim() || messageInput.value.trim().length < 10) {
        messageInput.classList.add('invalid');
        messageError?.classList.remove('hidden');
        isValid = false;
      } else {
        messageInput.classList.remove('invalid');
        messageError?.classList.add('hidden');
      }
      
      return isValid;
    }
    
    // Real-time validation
    const inputs = ['name', 'email', 'message'];
    inputs.forEach(inputId => {
      const input = document.getElementById(inputId);
      input?.addEventListener('blur', validateForm);
      input?.addEventListener('input', () => {
        // Clear error state on input
        input.classList.remove('invalid');
        const errorElement = document.getElementById(`${inputId}-error`);
        errorElement?.classList.add('hidden');
      });
    });
    
    // Form submission
    contactForm?.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      // Hide previous messages
      successMessage?.classList.add('hidden');
      errorMessage?.classList.add('hidden');
      
      if (!validateForm()) {
        return;
      }
      
      // Show loading state
      submitBtn.disabled = true;
      submitText?.classList.add('hidden');
      submitLoading?.classList.remove('hidden');
      
      try {
        // Simulate form submission (replace with actual API call)
        await new Promise(resolve => setTimeout(resolve, 2000));
        
        // Success
        successMessage?.classList.remove('hidden');
        contactForm.reset();
        
        // Scroll to success message
        successMessage?.scrollIntoView({ behavior: 'smooth', block: 'center' });
        
      } catch (error) {
        // Error
        errorMessage?.classList.remove('hidden');
        console.error('Form submission error:', error);
      } finally {
        // Reset button state
        submitBtn.disabled = false;
        submitText?.classList.remove('hidden');
        submitLoading?.classList.add('hidden');
      }
    });
    
    // Form reset
    contactForm?.addEventListener('reset', () => {
      // Clear all validation states
      inputs.forEach(inputId => {
        const input = document.getElementById(inputId);
        const errorElement = document.getElementById(`${inputId}-error`);
        input?.classList.remove('invalid');
        errorElement?.classList.add('hidden');
      });
      
      // Hide messages
      successMessage?.classList.add('hidden');
      errorMessage?.classList.add('hidden');
    });
    
    // Intersection Observer for animations
    const animatedElements = document.querySelectorAll('.animate-in');
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, {
      threshold: 0.1,
      rootMargin: '50px 0px'
    });
    
    animatedElements.forEach((element) => {
      observer.observe(element);
    });
    
    // Smooth scrolling for anchor links
    const anchorLinks = document.querySelectorAll('a[href^="#"]');
    anchorLinks.forEach(link => {
      link.addEventListener('click', (e) => {
        e.preventDefault();
        const targetId = link.getAttribute('href')?.substring(1);
        if (targetId) {
          const targetElement = document.getElementById(targetId);
          targetElement?.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
  });
</script>
